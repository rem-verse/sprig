# Test Binary Data #

Binary data used for testing certain parts of encryption/decryption with real
life files. These are small (<5mbs) to keep git happy (as git does not handle
even medium sized binary files well).

These files are not guaranteed to be usable in any context outside of these
tests. For example the decrypted firmware files used in for validating
decrypted contents are both invalid.
